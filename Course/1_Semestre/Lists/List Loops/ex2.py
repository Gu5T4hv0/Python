# Make an algorithm that shows the sum of the two lowest positive numbers, given a list of at least 4 positive integers.
numbers = [19, 5, 42, 2, 77]
numbers.sort()
summing = numbers[0] + numbers[1]
print(summing)